import { Position, SlidePage } from "../../state";

type Params = {
  diff: Position
}

/**
 * グループを解除する
 * @param state スライドページの状態
 * @param params パラメータ
 */
const moveShape = (state: SlidePage, params: Params): SlidePage => {
  const selectedShapes = 
}