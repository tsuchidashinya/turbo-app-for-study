import { Position } from "../../state"

type Rectangle = {
  width: number
  height: number
}

/**
 * 矩形の頂点を取得する
 * @param rectangle 図形のサイズに関するパラメータ
 */
const getRectVertices = (
  rectangle: Rectangle,
  centerPos: Position
): Position[] => {
  /** コードを記述 **/
}

export { getRectVertices }
